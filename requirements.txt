arrow==1.3.0
asgiref==3.8.1
certifi==2024.8.30
chardet==5.2.0
charset-normalizer==3.4.2
Pillow==10.4.0
python-dotenv==1.0.0
dj-database-url==3.0.0
Django==5.2.3
gunicorn==23.0.0
idna==3.10
openpyxl==3.1.2
packaging==25.0
psycopg2-binary==2.9.10
python-dateutil==2.9.0.post0
python-slugify==8.0.4
pytz==2024.1
requests==2.32.3
six==1.17.0
sqlparse==0.5.3
typing_extensions==4.14.0
tzdata==2024.1
urllib3==2.2.3
whitenoise==6.9.0

# IMAP/SMTP 이메일 암호화
cryptography==43.0.3

# Celery (비동기 작업 및 주기적 작업)
celery==5.4.0
redis==5.2.0

# OpenAI (AI PainPoint 분석)
openai>=1.68.0

# Cloudinary 스토리지는 더는 쓰지 않는다(업로드는 /data 볼륨 + FileSystemStorage).
# 그런데 마이그레이션 0067/0072 가 모듈 로드 시점에 `import cloudinary.models` 를 하고,
# 프로덕션은 부팅할 때마다 `manage.py migrate` 를 돌린다 — 이 패키지를 빼면 앱이 아예
# 안 뜬다. 두 마이그레이션을 손대는 건 이미 적용된 이력을 고치는 일이라 위험 대비
# 이득이 없어 패키지만 남겨 둔다. django-cloudinary-storage 는 참조가 없어 제거함.
cloudinary==1.41.0

# HTML Sanitizer (주간보고 리치 텍스트 에디터 서버 사이드 정화)
bleach==6.2.0


