# AI 기능 테스트 시나리오

## 📋 테스트 준비사항

### 1. 사용자 권한 확인

```python
# Django Admin에서 사용자의 can_use_ai 필드를 True로 설정
# 또는 Django shell에서:
from django.contrib.auth import get_user_model
User = get_user_model()
user = User.objects.get(username='your_username')
user.can_use_ai = True
user.save()
```

### 2. 환경변수 확인

`.env` 파일에 다음이 설정되어 있는지 확인:

```
OPENAI_API_KEY=sk-...
OPENAI_MODEL_MINI=gpt-4o-mini
OPENAI_MODEL_STANDARD=gpt-4o
```

---

## ✅ Feature 1: 고객 분석 & 요약 (기존 기능)

### 테스트 위치

- 고객 상세 페이지 (`/reporting/followup/<고객ID>/`)

### 테스트 순서

1. **AI 요약 생성**

   - 고객 상세 페이지 접속
   - "AI 인사이트" 섹션에서 "AI 요약 생성" 버튼 클릭
   - ✅ 로딩 중 표시 확인
   - ✅ 고객의 최근 활동 요약이 표시되는지 확인
   - ✅ 주요 포인트, 추천 액션이 포함되어 있는지 확인

2. **등급 분석**
   - 같은 페이지에서 "등급 분석" 버튼 클릭
   - ✅ 고객 등급(A+, A, B, C, D) 배지 표시 확인
   - ✅ 점수(0-100) 프로그레스 바 확인
   - ✅ 평가 근거 텍스트 확인
   - ✅ 참여도, 구매가능성, 금액, 최근성 점수 차트 확인
   - ✅ 추천 사항 리스트 확인

### 예상 결과

- 고객의 미팅, 견적, 납품 이력이 분석됨
- 등급은 활동 패턴에 따라 자동 산정됨
- 구체적인 액션 아이템 제공

---

## ✅ Feature 2: 미팅 노트 자동 요약

### 테스트 위치

- 캘린더 페이지 (`/reporting/calendar/`)

### 테스트 순서

1. **미팅 노트 작성**

   - 캘린더에서 미팅 일정 생성 또는 기존 미팅 클릭
   - 노트 내용을 100자 이상 작성 (예시 아래 참고)
   - "AI 요약" 버튼이 표시되는지 확인

2. **AI 요약 실행**
   - "AI 요약" 버튼 클릭
   - ✅ "🤖 AI가 노트를 요약하고 있습니다..." 메시지 표시
   - ✅ 3-5초 후 요약된 내용으로 노트가 교체됨
   - ✅ 주요 포인트, 액션 아이템, 다음 단계가 포함됨

### 테스트용 미팅 노트 예시

```
오늘 서울대학교 화학과 김교수님과 미팅했습니다.
신규 연구실 오픈 예정이라 HPLC 시스템 도입을 검토 중이십니다.
예산은 약 5천만원 정도 책정되어 있고, 다음달까지 결정하신다고 합니다.
경쟁사로는 A사와 B사가 있는데 우리 제품의 유지보수 서비스에 관심이 많으십니다.
다음주에 데모 일정을 잡기로 했고, 견적서는 이번주 금요일까지 보내드리기로 했습니다.
소모품도 함께 견적에 포함해달라고 하셨습니다.
```

### 예상 결과

```
[요약]
서울대 화학과 김교수님 미팅 완료. HPLC 시스템 도입 검토 중 (예산 5천만원, 다음달 결정).

[주요 포인트]
- 신규 연구실 오픈 예정
- 경쟁사: A사, B사
- 차별화 포인트: 유지보수 서비스

[액션 아이템]
- 금요일까지 견적서 발송 (소모품 포함)
- 다음주 데모 일정 확정

[다음 단계]
견적 검토 후 데모 진행 → 구매 결정
```

---

## ✅ Feature 3: 팔로우업 우선순위 제안

### 테스트 위치

- API 엔드포인트 직접 호출 또는 개발자 도구 콘솔

### 테스트 순서 (개발자 도구 콘솔 사용)

```javascript
// 1. 고객 목록 페이지에서 F12 눌러 콘솔 열기
// 2. 다음 코드 실행:

fetch("/reporting/ai/suggest-follow-ups/", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "X-CSRFToken": document.querySelector("[name=csrfmiddlewaretoken]").value,
  },
  body: JSON.stringify({}),
})
  .then((r) => r.json())
  .then((data) => {
    console.table(data.suggestions);
    console.log("총", data.total_analyzed, "명 분석");
  });
```

### 확인 사항

- ✅ 최대 20명의 고객이 우선순위순으로 정렬됨
- ✅ 각 고객마다 priority_score (1-100) 있음
- ✅ priority_level (urgent/high/medium/low) 있음
- ✅ reason (우선순위 이유) 있음
- ✅ suggested_action (제안 액션) 있음
- ✅ best_contact_time (연락 적기) 있음

### 예상 결과 예시

```json
{
  "customer_name": "김교수",
  "priority_score": 92,
  "priority_level": "urgent",
  "reason": "A+ 등급 고객, 마지막 연락 30일 경과, 진행 중인 기회 2건",
  "suggested_action": "즉시 전화 연락하여 진행 상황 확인",
  "best_contact_time": "오전 10-11시 (통화 이력 기반)"
}
```

---

## ✅ Feature 4: 이메일 스레드 감정 분석

### 테스트 위치

- API 엔드포인트 직접 호출

### 전제 조건

- Gmail 연동이 되어 있고, EmailLog에 이메일 데이터가 있어야 함
- thread_id가 같은 이메일이 여러 개 있어야 함

### 테스트 순서

```javascript
// 1. 먼저 thread_id 확인 (Django shell 또는 Admin에서)
// 2. 개발자 도구 콘솔에서:

fetch("/reporting/ai/analyze-email-thread/", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "X-CSRFToken": document.querySelector("[name=csrfmiddlewaretoken]").value,
  },
  body: JSON.stringify({
    thread_id: "your_thread_id_here",
  }),
})
  .then((r) => r.json())
  .then((data) => {
    console.log("분석 결과:", data.analysis);
    console.log("이메일 수:", data.email_count);
  });
```

### 확인 사항

- ✅ overall_sentiment: positive/neutral/negative
- ✅ temperature: hot/warm/cold (고객 온도)
- ✅ purchase_probability: high/medium/low
- ✅ engagement_level: high/medium/low
- ✅ key_topics: 주요 주제 배열
- ✅ concerns: 고객 우려사항
- ✅ opportunities: 발견된 기회
- ✅ next_action: 다음 액션 제안
- ✅ summary: 스레드 요약

### 예상 결과 예시

```json
{
  "overall_sentiment": "positive",
  "temperature": "hot",
  "purchase_probability": "high",
  "engagement_level": "high",
  "key_topics": ["가격 협상", "납기 확인", "기술 지원"],
  "concerns": ["예산 초과 우려", "납기 준수 여부"],
  "opportunities": ["추가 소모품 구매 가능성", "장기 계약 전환"],
  "next_action": "가격 조정안과 납기 확약서를 포함한 최종 제안서 발송",
  "summary": "고객이 제품에 매우 긍정적이나 가격과 납기에 대한 확신이 필요함. 즉시 대응 시 계약 가능성 높음."
}
```

---

## ✅ Feature 5: 상품 추천 (NEW!)

### 테스트 위치

- 고객 상세 페이지 (`/reporting/followup/<고객ID>/`)

### 테스트 순서

1. **준비**

   - 구매 이력이 있는 고객 선택 (DeliveryItem이 있어야 함)
   - 미팅 노트가 있는 고객이면 더 좋음

2. **추천 실행**

   - 고객 상세 페이지 하단 "AI 상품 추천" 섹션 확인
   - "추천 상품 보기" 버튼 클릭
   - ✅ 로딩 표시 확인
   - ✅ 추천 상품 카드들이 표시됨
   - ✅ 각 카드마다 우선순위 배지 있음 (높음/보통/낮음)
   - ✅ 추천 이유가 명확히 설명됨
   - ✅ 관련 소모품 제안 포함

3. **추천 품질 확인**
   - ✅ 과거 구매 제품과 연관성 있는지
   - ✅ 미팅 노트 내용과 관련 있는지
   - ✅ 업종/부서에 맞는 제품인지

### 예상 결과 예시

```
┌─────────────────────────────────────┐
│ 🔴 높음                              │
│ HPLC 컬럼 (C18, 5μm)                │
│ 카테고리: 소모품                     │
│                                     │
│ 이유: 6개월 전 HPLC 시스템을 구매   │
│ 하셨고, 일반적으로 3-6개월마다      │
│ 컬럼 교체가 필요합니다.             │
│                                     │
│ 함께 추천:                          │
│ • HPLC 용매                         │
│ • 필터 키트                         │
└─────────────────────────────────────┘
```

### API 직접 테스트

```javascript
fetch("/reporting/ai/recommend-products/123/", {
  // 123을 실제 고객 ID로 변경
  method: "GET",
  headers: {
    "X-CSRFToken": document.querySelector("[name=csrfmiddlewaretoken]").value,
  },
})
  .then((r) => r.json())
  .then((data) => {
    console.log("추천 상품:", data.recommendations);
    console.log("분석 데이터:", {
      구매이력: data.purchase_count,
      미팅수: data.meeting_count,
    });
  });
```

---

## ✅ Feature 6: 자연어 검색 (NEW!)

### 테스트 위치

- 고객 목록 페이지 (`/reporting/followup/`)

### 테스트 순서

1. **UI 확인**

   - 고객 목록 페이지 상단에 "AI 자연어 검색" 섹션 있는지 확인
   - Beta 배지 표시 확인
   - 예시 플레이스홀더 텍스트 확인

2. **검색 테스트 케이스**

#### 테스트 케이스 1: 날짜 기반 검색

```
검색어: "지난달 견적 준 고객"
```

- ✅ 지난달 견적 일정이 있는 고객 검색됨
- ✅ AI 해석 메시지 표시
- ✅ 결과 개수 표시

#### 테스트 케이스 2: 등급 + 기간 검색

```
검색어: "3개월 이상 연락 안 한 A등급 고객"
```

- ✅ A등급 고객 중 3개월 이상 연락 없는 고객 표시
- ✅ 마지막 연락일 표시

#### 테스트 케이스 3: 업종 + 활동 검색

```
검색어: "최근 3개월 미팅한 대학교 고객"
```

- ✅ 회사명에 "대학" 포함된 고객 필터링
- ✅ 최근 3개월 미팅 일정 있는 고객만

#### 테스트 케이스 4: 복합 조건

```
검색어: "견적 후 미구매 고객"
```

- ✅ 견적 일정은 있지만 납품 일정 없는 고객

#### 테스트 케이스 5: 금액 기반

```
검색어: "영업기회 금액 1000만원 이상"
```

- ✅ 영업기회 섹션에 결과 표시
- ✅ 금액 정렬되어 표시

3. **검색 결과 확인**
   - ✅ 고객, 일정, 영업기회 섹션으로 구분됨
   - ✅ 각 결과 클릭 시 해당 페이지로 이동
   - ✅ 총 결과 개수 표시
   - ✅ 결과 없을 때 적절한 메시지 표시

### API 직접 테스트

```javascript
fetch("/reporting/ai/search/", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "X-CSRFToken": document.querySelector("[name=csrfmiddlewaretoken]").value,
  },
  body: JSON.stringify({
    query: "지난달 견적 준 고객",
    search_type: "all", // 또는 'customers', 'schedules', 'opportunities'
  }),
})
  .then((r) => r.json())
  .then((data) => {
    console.log("AI 해석:", data.results.interpretation);
    console.log("고객:", data.results.customers);
    console.log("일정:", data.results.schedules);
    console.log("영업기회:", data.results.opportunities);
    console.log("총 결과:", data.total_count);
  });
```

---

## 🔍 통합 테스트 시나리오

### 시나리오: 신규 고객 발굴부터 계약까지

1. **자연어 검색으로 잠재고객 찾기**

   - "최근 6개월 미팅 없는 B등급 이상 고객" 검색
   - 우선순위 높은 고객 선택

2. **고객 상세 분석**

   - AI 요약 생성으로 고객 현황 파악
   - 등급 분석으로 구매 가능성 확인

3. **상품 추천 받기**

   - AI 상품 추천으로 제안 제품 선정
   - 추천 이유 참고하여 제안서 작성

4. **미팅 진행**

   - 캘린더에서 미팅 일정 생성
   - 미팅 후 상세 노트 작성
   - AI 요약으로 핵심 내용 정리

5. **이메일 분석**

   - 고객과 이메일 교환 후
   - 이메일 스레드 감정 분석
   - 고객 온도 파악 및 다음 액션 결정

6. **팔로우업 우선순위**
   - AI가 제안하는 팔로우업 타이밍 확인
   - 적기에 연락하여 계약 성사

---

## ⚠️ 일반적인 문제 해결

### 1. "AI 기능 사용 권한이 없습니다" 에러

**해결**: Django Admin에서 사용자의 `can_use_ai` 필드를 True로 설정

### 2. "OpenAI API 키 오류"

**해결**: `.env` 파일에 올바른 API 키 설정 확인

### 3. "요청 중 오류가 발생했습니다"

**해결**:

- 브라우저 개발자 도구 콘솔에서 에러 메시지 확인
- Railway 로그 확인: `railway logs`
- Django 로그 확인

### 4. "미팅 노트가 너무 짧습니다" (Feature 2)

**해결**: 최소 100자 이상 작성 필요

### 5. "추천 상품이 없습니다" (Feature 5)

**해결**:

- 고객의 구매 이력(DeliveryItem) 확인
- 미팅 노트 추가 작성

### 6. 자연어 검색 결과가 이상함 (Feature 6)

**해결**:

- 더 명확한 검색어 사용
- 날짜는 "지난달", "최근 3개월" 같은 상대적 표현 사용
- 등급, 일정 타입 등은 명확히 지정

---

## 📊 성능 확인

### API 응답 시간 측정

```javascript
async function measureAPITime(url, body) {
  const start = performance.now();
  const response = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "X-CSRFToken": document.querySelector("[name=csrfmiddlewaretoken]").value,
    },
    body: JSON.stringify(body),
  });
  const data = await response.json();
  const end = performance.now();
  console.log(`응답 시간: ${(end - start).toFixed(2)}ms`);
  return data;
}

// 사용 예시
measureAPITime("/reporting/ai/summarize-meeting/", {
  schedule_id: 123,
  notes: "테스트 노트 내용...",
});
```

### 예상 응답 시간

- Feature 1 (고객 요약): 3-8초
- Feature 2 (미팅 요약): 2-5초
- Feature 3 (우선순위): 5-15초 (고객 수에 따라)
- Feature 4 (이메일 분석): 4-10초
- Feature 5 (상품 추천): 3-7초
- Feature 6 (자연어 검색): 2-6초

---

## ✨ 추가 테스트 아이디어

### 엣지 케이스 테스트

1. **빈 데이터 테스트**

   - 구매 이력 없는 고객에게 상품 추천 요청
   - 미팅 노트 없는 일정에 AI 요약 시도

2. **대량 데이터 테스트**

   - 고객 100명 이상일 때 팔로우업 우선순위 (최대 50명까지만 분석됨)
   - 긴 미팅 노트 (5000자 이상) 요약

3. **동시성 테스트**

   - 여러 탭에서 동시에 AI 요청
   - 같은 고객에 대해 여러 AI 기능 동시 실행

4. **에러 핸들링 테스트**
   - 인터넷 연결 끊고 요청
   - 잘못된 고객 ID로 요청
   - CSRF 토큰 없이 요청

---

## 📝 테스트 체크리스트

- [ ] Feature 1: 고객 분석 & 요약 (기존)
  - [ ] AI 요약 생성
  - [ ] 등급 분석
- [ ] Feature 2: 미팅 노트 자동 요약
  - [ ] 100자 이상 노트 작성
  - [ ] AI 요약 버튼 클릭
  - [ ] 요약 결과 확인
- [ ] Feature 3: 팔로우업 우선순위
  - [ ] API 호출
  - [ ] 우선순위 점수 확인
  - [ ] 추천 액션 확인
- [ ] Feature 4: 이메일 스레드 분석
  - [ ] thread_id로 분석
  - [ ] 감정/온도 확인
  - [ ] 다음 액션 제안 확인
- [ ] Feature 5: 상품 추천
  - [ ] UI 버튼 표시
  - [ ] 추천 결과 카드
  - [ ] 우선순위 배지
  - [ ] 관련 소모품
- [ ] Feature 6: 자연어 검색
  - [ ] UI 검색창 표시
  - [ ] 다양한 검색어 테스트
  - [ ] 결과 카테고리 구분
  - [ ] 클릭 동작 확인

---

## 🔄 전체 고객 등급 일괄 업데이트

### Management Command 사용법

전체 고객의 AI 등급을 일괄로 업데이트하려면 Django management command를 사용하세요.

#### 기본 사용법

```bash
# 전체 고객 등급 업데이트
python manage.py update_all_customer_grades
```

#### 옵션

```bash
# 최대 50명만 업데이트
python manage.py update_all_customer_grades --limit 50

# 특정 등급의 고객만 업데이트 (A+, A 등급)
python manage.py update_all_customer_grades --grade A+,A

# 실제 업데이트 없이 결과만 미리보기 (DRY RUN)
python manage.py update_all_customer_grades --dry-run

# 최소 활동 1회 이상인 고객만 (미팅+견적+납품)
python manage.py update_all_customer_grades --min-activity 1

# 옵션 조합
python manage.py update_all_customer_grades --limit 100 --grade A+,A,B --dry-run
```

#### 출력 예시

```
======================================================================
AI 기반 전체 고객 등급 업데이트 시작
======================================================================
등급 필터: A+, A
최소 활동 횟수: 1회 이상
업데이트 대상: 최대 50명

총 50명의 고객을 처리합니다.

[1/50] 처리 중: 김교수 (서울대학교)
  🔄 등급: A → A+ (점수: 92/100)
  📝 근거: 최근 3개월간 3회 미팅, 견적 2건 진행 중, 구매 이력 우수...

[2/50] 처리 중: 이연구원 (한국과학기술원)
  ✓ 등급: A → A (점수: 85/100)
  📝 근거: 정기적 구매, 응답성 높음, 추가 구매 가능성...

...

======================================================================
업데이트 완료
======================================================================

총 처리: 50명
✓ 성공: 48명
✗ 실패: 2명
🔄 등급 변경: 12명
⏱️  소요 시간: 87.3초

======================================================================
등급 변경 상세:
======================================================================
  • 김교수: A → A+ (92점)
  • 박박사: B → A (88점)
  • 최교수: A+ → A (83점)
  ...

======================================================================
업데이트 후 등급 분포:
======================================================================
  A+: 15명
  A: 28명
  B: 32명
  C: 18명
  D: 7명
```

#### 주의사항

⚠️ **OpenAI API 비용**

- 고객 1명당 약 0.02-0.05 USD 소요
- 100명 업데이트 시 약 $2-5 예상
- `--dry-run`으로 먼저 테스트 권장

⚠️ **실행 시간**

- 고객 1명당 약 2-3초 소요
- 100명 업데이트 시 약 3-5분 소요
- API 속도 제한으로 자동 대기 시간 포함

⚠️ **권장 사용 시기**

- 월 1회 정기 업데이트
- 신규 고객 대량 등록 후
- 주요 캠페인 전후
- 분기별 고객 등급 재평가

#### 자동화 설정 (선택사항)

cron으로 월 1회 자동 업데이트 설정:

```bash
# crontab -e
# 매월 1일 오전 3시에 실행
0 3 1 * * cd /path/to/sales-note && python manage.py update_all_customer_grades --min-activity 1
```

---

## 🎯 테스트 완료 후

### 1. 로그 확인

```bash
# Railway 로그 확인
railway logs

# 또는 로컬 Django 로그
tail -f logs/django.log
```

### 2. 비용 모니터링

- OpenAI API 사용량 확인: https://platform.openai.com/usage
- 토큰 사용량 최적화 여부 확인

### 3. 사용자 피드백 수집

- 어떤 기능이 가장 유용한가?
- 응답 속도는 적절한가?
- 추천/분석 품질은 만족스러운가?

### 4. 등급 데이터 확인

```python
# Django shell에서 등급 분포 확인
from reporting.models import FollowUp
from django.db.models import Count

FollowUp.objects.values('customer_grade').annotate(
    count=Count('id')
).order_by('-customer_grade')
```

---

**테스트 성공을 기원합니다! 🚀**
